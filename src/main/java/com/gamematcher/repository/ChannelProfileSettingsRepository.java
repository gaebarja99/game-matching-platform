package com.gamematcher.repository;

import com.gamematcher.entity.ChannelProfileSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChannelProfileSettingsRepository extends JpaRepository<ChannelProfileSettings, Long> {
}
