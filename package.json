{
  "name": "gamematcher",
  "private": true,
  "description": "GameMatcher main workspace",
  "scripts": {
    "start": "npm run dev --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
